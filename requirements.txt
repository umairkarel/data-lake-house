# ---------------------------------------------------------------------------
# Python dependencies for the Lake-House project
# ---------------------------------------------------------------------------

# PyFlink — must match FLINK_VERSION in docker/flink/Dockerfile
apache-flink==1.20.0

# PyIceberg — Iceberg table operations, catalog management, compaction
# [s3fs]    adds s3fs for direct MinIO reads from local scripts
# [nessie]  adds pynessie as the catalog backend
pyiceberg[s3fs,nessie]==0.8.1

# Nessie client — branch/tag/merge operations (installed transitively via pyiceberg[nessie])
pynessie

# DuckDB — query Iceberg tables locally via PyArrow integration
duckdb

# YAML config loading
pyyaml

# CLI / scripting utilities
boto3          # optional: direct MinIO S3 operations from Python
s3fs           # fsspec S3 filesystem (used by PyIceberg s3 extra)

# Arrow (required by PyIceberg scan → DuckDB)
pyarrow>=14.0.0
